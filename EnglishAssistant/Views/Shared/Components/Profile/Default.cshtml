@model EnglishAssistant.ViewModels.Shared.Components.ProfileViewModel;

<div class="profile">
    <ul>
        @if (Model.IsAuthenticated)
        {
            <li><a href="#">@Model.Username</a></li>
            <li><a href="@Url.Action("SignOut", "Account")">Sign out</a></li>
        } else
        {
            <li><a href="@Url.Action("Create", "Account")">Sign up</a></li>
            <li><a href="@Url.Action("Login", "Account")">Sign in</a></li>
        }
    </ul>
</div>